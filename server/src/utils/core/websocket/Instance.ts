export function handleInstanceCommand(data: any): string {
    // Process the data and return the response
    // For demonstration purposes, we'll just return a simple message
    // You can customize this to perform any action required by the "instance" command
  
    return `Handled instance command with data: ${JSON.stringify(data)}`;
  }